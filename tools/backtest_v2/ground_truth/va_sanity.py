"""SANITY-ONLY ground truth: V-A proximity floor (tagged engine-derived). §7 — Phase 1.

Engine-derived: use only as a sanity floor, never as headline accuracy.
"""

from __future__ import annotations

from typing import Any


def build_va_sanity_gt(*args: Any, **kwargs: Any):
    raise NotImplementedError("build_va_sanity_gt — Phase 1")
