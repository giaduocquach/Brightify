"""JSON report writer (machine-readable, with validity labels). §8 — Phase 1."""

from __future__ import annotations

from typing import Any


def write_json(report: Any, path: str) -> None:
    raise NotImplementedError("write_json — Phase 1")
